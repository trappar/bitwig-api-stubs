/**
 * Must be called for the API to work
 * @param {number} version
 */
function loadAPI(version){}

/**
 * Load another script into this script (include)
 * @param {string} script - path to script
 */
function load(script){}