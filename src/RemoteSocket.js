function RemoteSocket(){}

/**
 * Set a callback which receives a RemoteConnection object for each incoming connection.
 * @param {function} callback
 */
RemoteSocket.prototype.setClientConnectCallback = function(callback){};